{
  "compilerOptions": {
    // ESSENCIAL: Diz ao TypeScript para compilar 'import' para 'require' (ES Modules -> CommonJS).
    "module": "CommonJS",
    
    // Alvo de saída do JavaScript (versão moderna)
    "target": "ES2020",
    
    // RECOMENDADO: Garante que o TypeScript use as regras de resolução de módulos do Node.js.
    "moduleResolution": "Node",
    
    // Permite que módulos ES sejam tratados como CJS, crucial para muitas dependências
    "esModuleInterop": true,
    
    // CORRIGIDO: O "rootDir" agora aponta para o diretório atual do projeto.
    // Isso é importante para o ts-node e para a estrutura de pastas.
    "rootDir": "./", 
    
    // Pasta onde o JavaScript compilado será gerado (usado apenas se "noEmit" for "false")
    "outDir": "./dist", 
    
    // Configurações de tipagem rigorosa (boa prática)
    "strict": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    
    // Mantido como "true" para usar o ts-node sem gerar arquivos .js no disco.
    "noEmit": true 
  },
  // CORRIGIDO: Inclui todos os arquivos .ts (e .tsx) na raiz e em todas as subpastas.
  "include": ["**/*.ts", "**/*.tsx", "../frontend-app/src/firebase.ts"], 
  
  // Exclui as pastas que não contêm código-fonte a ser processado.
  "exclude": ["node_modules", "dist"]
}